<%= form_for @portfolio, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :cash_holding, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :cash_holding, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :net_worth, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :net_worth, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  portfolios_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
